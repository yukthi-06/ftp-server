# User Prompt
on settings page, two buttons have text overflowing because of less height of buttons. fix it

# Walkthrough - Fix Settings Buttons Text Overflow

We have resolved the text overflow issue affecting the action buttons at the bottom of the settings screen.

## Changes Made

### Layout Refactoring
- Modified [activity_settings.xml](file:///x:/Projects_X/0_Active/8_Android_APK/05_Early_Stage/FTP_Server_APK_GITHUB/app/src/main/res/layout/activity_settings.xml):
  - Changed `android:layout_height` from `56dp` to `wrap_content` for both `@id/btnReset` and `@id/btnSave` buttons.
  - Added `android:minHeight="56dp"` to both buttons to guarantee they retain a clean, consistent height and adequate touch target size while allowing text to scale or wrap without clipping.

## Verification
- Verified layout parameters and code syntax.
