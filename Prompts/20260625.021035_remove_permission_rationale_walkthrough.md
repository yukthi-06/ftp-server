# User Prompt
on app startup, when asking permissions, there is a messge before File access permission is sought.  That is unnecesary.  Directly go to file access permission window

# Walkthrough - Directly Request File Access Permission

We have bypassed the explanatory intermediate dialog that was displayed prior to requesting the "All Files Access" (`MANAGE_EXTERNAL_STORAGE`) permission on Android 11+ devices.

## Changes Made

### Main Activity
- Modified [MainActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/05_Early_Stage/FTP_Server_APK_GITHUB/app/src/main/java/com/vypeensoft/ftpserver/MainActivity.java):
  - Removed the custom `AlertDialog.Builder` construction and lifecycle code in `checkAllFilesAccess()`.
  - Refactored the method to directly attempt starting the system settings intents (`Settings.ACTION_MANAGE_APP_ALL_FILES_ACCESS_PERMISSION` or `Settings.ACTION_MANAGE_ALL_FILES_ACCESS_PERMISSION` fallback) on Android 11+.

## Verification
- Verified syntax, logic flow, and build integrity.
