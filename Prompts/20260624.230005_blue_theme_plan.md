# User Prompt
instead of black text and borders, make it a blue text and borders.
Keep the Start button green and Stop button in red color

# Implementation Plan - Blue Theme with Green/Red Action Buttons

We will update the application theme, custom color palette, and layout files to implement a blue theme (blue text and borders) on a white background, while keeping the Start button green and the Stop button red.

## User Review Required

> [!IMPORTANT]
> The primary color (`primaryColor`) is updated to a solid blue color (`#1976D2`). Layout borders and text titles will be changed from `@color/black` to `@color/primaryColor`. The start and stop statuses and buttons will remain green (`#4CAF50`) and red (`#F44336`) respectively.

## Proposed Changes

### Colors & Themes

#### [MODIFY] [colors.xml](file:///x:/Projects_X/0_Active/8_Android_APK/05_Early_Stage/FTP_Server_APK_GITHUB/app/src/main/res/values/colors.xml)
- Set `primaryColor` to Blue (`#1976D2`).
- Set `primaryDarkColor` to Dark Blue (`#1565C0`).
- Set `primaryLightColor` to Light Blue (`#BBDEFB`).
- Restore `statusRunning` to Green (`#4CAF50`).
- Restore `statusStopped` to Red (`#F44336`).

#### [MODIFY] [themes.xml](file:///x:/Projects_X/0_Active/8_Android_APK/05_Early_Stage/FTP_Server_APK_GITHUB/app/src/main/res/values/themes.xml)
- Set `colorOnPrimary` to `@color/primaryColor` (so the title text on the white toolbar becomes blue).

### Layout Adjustments

#### [MODIFY] [activity_main.xml](file:///x:/Projects_X/0_Active/8_Android_APK/05_Early_Stage/FTP_Server_APK_GITHUB/app/src/main/res/layout/activity_main.xml)
- Change toolbar title text color to `@color/primaryColor`.
- Set card view stroke color and text elements to `@color/primaryColor`.
- Change navigation icon tint to `@color/primaryColor`.
- Keep `btnStart` and `btnStop` background tints linked to `@color/statusRunning` (green) and `@color/statusStopped` (red).

#### [MODIFY] [activity_settings.xml](file:///x:/Projects_X/0_Active/8_Android_APK/05_Early_Stage/FTP_Server_APK_GITHUB/app/src/main/res/layout/activity_settings.xml)
- Set toolbar title text color, back navigation icon tint, and button borders to `@color/primaryColor`.
- Style primary save button to solid blue (`@color/primaryColor`).

#### [MODIFY] [activity_about.xml](file:///x:/Projects_X/0_Active/8_Android_APK/05_Early_Stage/FTP_Server_APK_GITHUB/app/src/main/res/layout/activity_about.xml)
- Change toolbar title, icon tint, and card stroke to `@color/primaryColor`.

#### [MODIFY] [activity_help.xml](file:///x:/Projects_X/0_Active/8_Android_APK/05_Early_Stage/FTP_Server_APK_GITHUB/app/src/main/res/layout/activity_help.xml)
- Change toolbar title, icon tint, and card stroke to `@color/primaryColor`.
- Change email support button text/icon tint to `@color/primaryColor`.

## Verification Plan

### Automated Tests
- Build application: `./gradlew assembleDebug`

### Manual Verification
- Deploy to test device/emulator and verify that all borders and primary text are blue on a white background, while the Start button is green and the Stop button is red.
