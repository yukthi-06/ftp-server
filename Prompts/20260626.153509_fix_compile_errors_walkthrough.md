# Walkthrough - Fix Compilation Errors

We have successfully resolved the compilation errors in `FTPServerManager.java`. Below is a summary of the changes implemented.

## Changes Made

### Files Modified
- [FTPServerManager.java](file:///x:/Projects_X/0_Active/8_Android_APK/02_Stable/FTP_Server_APK_GITHUB/app/src/main/java/com/vypeensoft/ftpserver/FTPServerManager.java)

### Key Improvements
1. **Corrected Import Statement**:
   - Updated the import of `NativeFileSystemFactory` to `org.apache.ftpserver.filesystem.nativefs.NativeFileSystemFactory` (instead of the internal `.impl` package).
2. **Fixed Generics Assignment**:
   - Declared the list returned by `file.listFiles()` as `List<? extends FtpFile>` to resolve the incompatible types error.

## Verification
- Verified that the source changes compile cleanly.
