# User Prompt
> Task :app:compileDebugJavaWithJavac
... Compilation errors in FTPServerManager.java

# Task Journal - Fix Compilation Errors

## Initial Analysis
- `NativeFileSystemFactory` is imported from the wrong package.
- `file.listFiles()` returns `List<? extends FtpFile>`.

## Design Decisions
- Correct the package import for `NativeFileSystemFactory`.
- Update the list variable declaration for `files`.

## Implementation Plan
1. Fix imports and variable type in `FTPServerManager.java`.

## Implementation Summary

### Files Actually Modified
- [FTPServerManager.java](file:///x:/Projects_X/0_Active/8_Android_APK/02_Stable/FTP_Server_APK_GITHUB/app/src/main/java/com/vypeensoft/ftpserver/FTPServerManager.java)

### Decisions Made During Implementation
- Corrected the import of `NativeFileSystemFactory` to `org.apache.ftpserver.filesystem.nativefs.NativeFileSystemFactory`.
- Declared the list returned by `file.listFiles()` as `List<? extends FtpFile>` to match its generic type bounds.

### Deviations from the Original Plan
- None.

### Outstanding Issues
- None.

### Suggested Next Steps
- Re-run compilation to verify success.

